# Application URL (public URL used by clients)
APP_URL=https://ameri.boo

# In-container TLS configuration (nginx + certbot)
DOMAIN=ameri.boo
LETSENCRYPT_EMAIL=metaranha@gmail.com
# Set to 1 for Let's Encrypt staging certs during testing to avoid rate limits.
LETSENCRYPT_STAGING=0

# Database (managed/external PostgreSQL recommended)
DATABASE_URL=postgresql://user:password@host:5432/cotrainer

# Backend
SECRET_KEY=change_this_to_random_32_char_string
NOTION_API_KEY=your_notion_api_key_here
NOTION_DATABASE_ID=your_notion_database_id_here

# Generate SECRET_KEY with: openssl rand -hex 32
# Or in PowerShell: -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | ForEach-Object {[char]$_})
